"""
1. the _graph_ needs to know cost of movement
2. the _queue_ needs to return nodes in a different order
3. the _search_ needs to keep track of these costs from the
    graph and give the to the queue
"""
